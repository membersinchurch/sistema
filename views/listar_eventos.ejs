<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Lista de Eventos</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-6">
  <div class="max-w-4xl mx-auto bg-white p-6 rounded shadow">
    <h2 class="text-xl font-bold mb-4">Eventos Cadastrados</h2>

    <a href="/eventos/novo" class="bg-green-600 text-white px-4 py-2 rounded mb-4 inline-block">Novo Evento</a>

    <ul class="divide-y">
      <% eventos.forEach(evento => { %>
        <li class="py-2">
          <strong><%= evento.titulo %></strong> — <%= evento.data_inicio %> (<%= evento.categoria %>)
        </li>
      <% }) %>
    </ul>
  </div>
</body>
</html>
